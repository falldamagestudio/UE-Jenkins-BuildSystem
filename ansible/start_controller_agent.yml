---
- hosts: remote
  become: yes
  gather_facts: true
  pre_tasks:
  roles:
    - start_controller_agent_service
  tasks:
  vars_files:
    - "{{ env_path }}/env_vars.yml"
    - "{{ global_env_path }}/env_vars.yml"
