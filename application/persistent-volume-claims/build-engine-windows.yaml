apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: build-engine-windows
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: pd-balanced-ntfs
  resources:
    requests:
      storage: 300Gi
