credentials:
  system:
    domainCredentials:
    - credentials:
      - usernamePassword:
          id: github-user
          username: github-user
          password: ${GITHUB_PAT}
          description: "GitHub Personal Access Token"
          scope: GLOBAL
