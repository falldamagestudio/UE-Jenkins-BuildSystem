version: "2"

services:

  jenkins:
    volumes:
      - ../jenkins-master-config/config/google-auth:/var/jenkins_home/mounts/config/google-auth
      - ../jenkins-master-config/config/shared:/var/jenkins_home/mounts/config/shared
    environment:
      - GOOGLE_OAUTH_CLIENT_ID=${GOOGLE_OAUTH_CLIENT_ID}
      - GOOGLE_OAUTH_CLIENT_SECRET=${GOOGLE_OAUTH_CLIENT_SECRET}
