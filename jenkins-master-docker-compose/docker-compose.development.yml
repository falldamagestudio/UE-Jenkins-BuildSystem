version: "2"

services:

  jenkins:
    environment:
      - CASC_JENKINS_CONFIG=/var/jenkins_home/mounted-configs/config/development
      - JENKINS_ADMIN_USERNAME=${JENKINS_ADMIN_USERNAME}
      - JENKINS_ADMIN_PASSWORD=${JENKINS_ADMIN_PASSWORD}
  